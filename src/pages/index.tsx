import React, { FC } from "react";
import Home from "src/components/modules-components/Home";

const Index: FC = () => {
  return (
    <main className="relative min-h-full">
      <Home />
    </main>
  );
};

export default Index;
